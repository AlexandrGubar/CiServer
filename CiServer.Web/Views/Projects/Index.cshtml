@model IEnumerable<CiServer.Core.Entities.Project>
@{ ViewData["Title"] = "Всі проєкти"; }

<h2>Список проєктів</h2>
<a href="/Projects/Create" class="btn btn-primary mb-3">Додати новий проєкт</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Назва</th>
            <th>Репозиторій</th>
            <th>Дата створення</th>
            <th style="width: 250px;">Дії</th> </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a href="/Projects/Details/@item.ProjectId">@item.Name</a>
                </td>
                <td><a href="@item.RepoUrl" target="_blank">@item.RepoUrl</a></td>
                <td>@item.CreatedAt.ToLocalTime()</td> <td>
                    <div class="d-flex gap-2">
                         <form action="/Projects/StartBuild/@item.ProjectId" method="get">
                            <button type="submit" class="btn btn-warning btn-sm">🚀 Build</button>
                        </form>
                        
                        <a href="/Projects/Delete/@item.ProjectId" class="btn btn-danger btn-sm">🗑 Видалити</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>