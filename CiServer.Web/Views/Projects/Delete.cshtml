@model CiServer.Core.Entities.Project
@{ 
    ViewData["Title"] = "Видалити проєкт"; 
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Видалити проєкт - CI Server</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    
    <style>
        body { 
            background-color: #f8f9fa; 
            font-family: 'Segoe UI', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .delete-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(220, 53, 69, 0.15); /* Червона тінь */
            width: 100%;
            max-width: 500px;
            overflow: hidden;
            border: 1px solid #ffebeb;
        }
        .card-header-danger {
            background-color: #fff5f5;
            color: #dc3545;
            padding: 30px;
            text-align: center;
            border-bottom: 1px solid #ffe0e0;
        }
    </style>
</head>
<body>

    <div class="delete-card">
        <div class="card-header-danger">
            <i class="bi bi-exclamation-triangle-fill display-1 mb-3"></i>
            <h2 class="fw-bold mb-2">Ви впевнені?</h2>
            <p class="text-muted mb-0">Це незворотна дія. Проєкт буде видалено.</p>
        </div>
        
        <div class="p-4">
            <div class="alert alert-light border text-center mb-4">
                Ви збираєтесь видалити проєкт: <br>
                <strong>@Model.Name</strong>
            </div>
            
            <p class="text-center small text-muted mb-4">
                Разом з проєктом буде видалено всю історію збірок, логи та збережені артефакти.
            </p>

            <form action="/Projects/Delete" method="post">
                <input type="hidden" name="projectId" value="@Model.ProjectId" />
                
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-danger btn-lg fw-bold">
                        Так, видалити назавжди
                    </button>
                    <a href="/Projects" class="btn btn-light btn-lg text-muted">
                        Скасувати
                    </a>
                </div>
            </form>
        </div>
    </div>

</body>
</html>